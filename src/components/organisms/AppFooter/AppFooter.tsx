import { Box } from "@mantine/core";
import React from "react";

export const AppFooter = () => {
  return (
    <Box
      sx={{
        height: 200,
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        background: "#F6F6F6",
        borderTop: "1px solid #eee",
      }}
    >
      AppFooter - Content to be added later
    </Box>
  );
};
